import React, { Fragment } from 'react';

import InputForm from '../component/InputForm';

// will contain routing to different components later on

const Pages: React.SFC = () => {
  return (
    <Fragment>
      <InputForm />
    </Fragment>
  );
};

export default Pages;
